DATABASE_URL="file:../data/ifm.db"

IFM_DATA_DIR="./data"
IFM_UPLOAD_DIR="./data/uploads"

# IFM dev server (port 3001 — RSE uses 3000)
# IFM_DEV_PORT=3001

# Sibling RSE app URL for Import from RSE (RSE runs on port 3000)
RSE_API_URL="http://127.0.0.1:3000"
